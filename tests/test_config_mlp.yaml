# Test Configuration File for MLP CI CD

# log_dir: tests/Datasets/logs # put to data dir # output
# device: auto

dataset:
  dir: tests/Datasets/
  name: 'example'
  # file: 'loan'
  # column: 'Status'
  query: 'loan.Status'
  # type: 'graph'
  join: 0

train:
  epoch: 200

model:
  # type: 'MLP'
  name: 'GNN'
  hidden_dim: 128
  layer: 4

optim:
  lr: 0.001
  weight_decay: 0.001
