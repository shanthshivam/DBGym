# Test Configuration File for MLP CI CD

dataset:
  dir: tests/Datasets/
  name: 'example'
  query: 'loan.Status'

train:
  epoch: 200

model:
  name: 'GCN'
  hidden_dim: 128
  layer: 4

optim:
  lr: 0.001
  weight_decay: 0.001
